[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    integration: requires locally running worker services
    kaggle: requires a Kaggle GPU runtime
    public_tunnel: requires an active Cloudflare Quick Tunnel
